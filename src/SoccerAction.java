/**
 * Defines the set of actions our soccer agent can perform
 * 
 * Note: Ensure these properties are defined in the exact same order in the
 * properties file
 * 
 */
public enum SoccerAction {
    TURN, DASH, KICK
}